# git-livesync

This is a VS Code extension to watch for file changes and automatically commit to a local repository and optionally push/sync to a remote.

Uses https://www.npmjs.com/package/generator-code